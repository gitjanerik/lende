{
  "name": "lende-nve-proxy",
  "private": true,
  "version": "1.0.0",
  "description": "Cloudflare Worker som proxyer NVE HydAPI for Lende og holder API-nøkkelen server-side.",
  "scripts": {
    "dev": "wrangler dev",
    "deploy": "wrangler deploy"
  },
  "devDependencies": {
    "wrangler": "^4.0.0"
  }
}
