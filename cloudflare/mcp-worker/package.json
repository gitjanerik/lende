{
  "name": "lende-mcp",
  "private": true,
  "version": "1.0.0",
  "description": "Cloudflare Worker som eksponerer Lendes MCP-verktøy over Streamable HTTP (Spor 1), med samme per-bruker-tokens som lende-ai.",
  "scripts": {
    "dev": "wrangler dev",
    "deploy": "wrangler deploy"
  },
  "dependencies": {
    "@modelcontextprotocol/sdk": "^1.30.0",
    "agents": "^0.21.0",
    "zod": "^4.4.3"
  },
  "devDependencies": {
    "wrangler": "^4.0.0"
  }
}
